<UserControl x:Class="Panoptes.View.Panels.RuntimeStatisticsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Panoptes.View.Panels"
             xmlns:panels="clr-namespace:Panoptes.ViewModels.Panels;assembly=Panoptes.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=panels:RuntimeStatisticsPanelViewModel,IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary Source="StatisticsResourceDictionary.xaml"/>
    </UserControl.Resources>
    
    <Grid>
        <DataGrid IsReadOnly="True" GridLinesVisibility="None" AutoGenerateColumns="False" ItemsSource="{Binding Statistics}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="{StaticResource NameDataGridLength}"/>
                <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="{StaticResource ValueDataGridLength}"
                                    ElementStyle="{StaticResource StatisticValueTextBlockStyle}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
